# Required: Google AI Studio API key
GEMINI_API_KEY=YOUR_API_KEY

# Server
PORT=3000

# Model (default to native audio dialog per requirement)
GEMINI_MODEL=gemini-2.5-flash-preview-native-audio-dialog

# Optional: switch to dev models during testing to avoid free-tier limits
# GEMINI_MODEL=gemini-2.0-flash-live-001
# GEMINI_MODEL=gemini-live-2.5-flash-preview

# Optional: override the system prompt here; if empty, server/systemInstruction.txt is used
# SYSTEM_INSTRUCTION=You are Rev, Revolt Motors’ AI assistant. Only discuss Revolt Motors...
